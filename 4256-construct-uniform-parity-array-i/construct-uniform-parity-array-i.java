class Solution {
    public boolean uniformArray(int[] nums1) {
        return true;
        // as all number are even 
        // all number are odd
        // we can change parity of an even number to odd if num is mix of even and odd numbers as even-odd=odd
    }
}